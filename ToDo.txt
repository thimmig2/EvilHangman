

Index.html.erb
    check session[userId] exists
        if not redirect to login
        if it does
            display user history entries
            Have button "Start New Game" (redirects to


Login.html.erb three options :
    login with valid credentails
    register
    play as annonymous (redirect right to hangman.html.erb)



Game.controller
     start new game using game.model
     track changes and update game/view/hangman.html.erb accordingly
     add user history entry